{
  "name": "fantasy-manager-extension",
  "private": true,
  "version": "0.32.0",
  "description": "Dev tooling for the Fantasy Manager Chrome extension. The extension itself has no dependencies and is loaded unpacked — this file exists only so `node` treats the test files as ES modules and so the optional load check can install its browser driver.",
  "type": "module",
  "scripts": {
    "test": "bash test/run_all.sh",
    "load-check": "node test/load_check.js"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
